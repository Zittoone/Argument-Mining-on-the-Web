//Very important : write here the URL to your project root
let staticUrl = "/"; // j'ai pas pigé le static url ^^ utilise le realitf plutôt non ? (ça marchait pas chez moi avec localhost)

let nodeURL = staticUrl + "datasets/NoDE/";